export type CocosoDebugResults = {
  normalizedMatrix: number[][];
  weightedMatrix: number[][];
  weightedSums: number[];
  powerSums: number[];
  kia: number[];
  kib: number[];
  kic: number[];
  scores: number[];
};
