# MCDA.js - Angular Integration Example

This is an illustrative example of integrating MCDA.js library into an Angular application.

To run the application:

```bash
npm ci
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`.
