# MCDA.js - React Integration Example

This is an illustrative example of integrating MCDA.js library into a React application.

To run the application:

```bash
npm ci
npm run dev
```

Once the server is running, open your browser and navigate to `http://localhost:5173/`.
