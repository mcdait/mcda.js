# MCDA.js - ESM Integration Example

This is an illustrative example of integrating MCDA.js library directly into an ESM (ECMAScript Module) application.

To run the application, simply open the `index.html` file in your preferred web browser. No additional server setup is required for this example.
